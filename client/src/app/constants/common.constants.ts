export enum ESocketChannel {
    HasChange = 'HasChange'
}

export enum EStatusParking {
    PARKED = 'PARKED'
}

export enum EAccountModalType {
    Remove = 'remove',
    EditRole = 'edit-role',
    ChangePassword = 'change-password',
    Create = 'create'
}