export enum NotifyType {
    Info = 'info',
    Success = 'success',
    Warning = 'warning',
    Danger = 'danger',
    Primary = 'primary'
}

export enum NotifyPositionFrom {
    Top = 'top',
    Bottom = 'bottom'
}

export enum NotifyPositionAlign {
    Left = 'left',
    Right = 'right',
    Center = 'center'
}