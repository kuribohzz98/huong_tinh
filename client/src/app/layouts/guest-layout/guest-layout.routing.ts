import { Routes } from '@angular/router';
import { ManagerParkingComponent } from '../../pages/manager-parking/manager-parking.component';

export const GuestLayoutRoutes: Routes = [
    { path: '', component: ManagerParkingComponent }
];
